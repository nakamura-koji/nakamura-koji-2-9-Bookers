<body>
  <div class="notice">
   <%= notice %>
  </div>

  <%= form_with model:@book, local:true do |f| %>
    <div class="text">
      <strong>Title:</strong>
      <%= @book.title %>
    </div>

    <div class="text">
      <strong>Body:</strong>
      <%= @book.body %>
    </div>
  <%end%>
</body>

<%= link_to "Edit",edit_book_path(@book.id) %>
|
<%= link_to "Back",books_path %>
